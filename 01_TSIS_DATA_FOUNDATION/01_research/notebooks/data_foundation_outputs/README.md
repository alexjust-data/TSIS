# Data Foundation Output Notebooks

Research-only notebooks for inspecting CAPA 1 output tables and controlled
candidate replays.

These notebooks are not authoritative materializers. Authority remains in:

```text
01_foundations/
scripts/
configs/
tests/
manifests generated by each run
```

## Daily Scanner Candidates

Notebook:

```text
daily_scanner_candidates_replay_view_v0_1.ipynb
```

Use it to inspect controlled replays produced by:

```text
scripts/materialize_daily_scanner_candidates_table.py
```

Default replay evidence:

```text
C:/TSIS_Data/tests/test_runs/2026-06-29/daily_scanner_candidates_replay_20250102_20250110_v0_1/
```

Output-root policy:

```text
small samples / tests / demos:
  C:/TSIS_Data/tests/test_runs/<run_date>/<run_id>/

long-range candidate replays:
  E:/TSIS/data/data_foundation_outputs/daily_scanner_candidates_table/candidate_replays/<run_id>/

official promoted root, do not write directly from the notebook:
  E:/TSIS/data/data_foundation_outputs/daily_scanner_candidates_table/daily_scanner_candidates_table_v0_1/
```

Interpretation:

- `trade_station_like_scanner_v0_1` shows operational hot-list style
  visibility;
- `broad_in_play_discovery_scanner_v0_1` shows broad research discovery;
- scanner rows are candidate lineage only;
- scanner rows are not final market states, labels, rewards, strategy signals,
  fills, PnL or live authority.
