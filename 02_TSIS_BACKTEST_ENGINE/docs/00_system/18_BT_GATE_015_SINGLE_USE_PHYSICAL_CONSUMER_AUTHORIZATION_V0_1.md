# BT-GATE-015 Single-Use Physical Consumer Authorization Candidate

VERSION = `V0_1`
GATE = `BT-GATE-015`
STATUS = `DRAFT_SUPERSEDED_NEVER_AUTHORIZED`
EXECUTION_AUTHORIZED = `false`
CONSUMED_BY_RUN_ID = `null`

This historical candidate was created before the non-physical external review
was recorded. It never received a pre-execution review and must not be used.

```text
PHYSICAL_CONSUMER_READ = NOT_EXECUTED
PHYSICAL_STATE_ROWS_READ = 0
SECOND_USE = NOT_APPLICABLE
SUPERSEDED_BY = V0_2_DRAFT_CANDIDATE
```

The frozen slice was AAME / XNYS / 2021-01-19. Preserving this document does
not grant physical authority, StateReplayFeed, general Event State consumption,
strategy execution, orders, fills or PnL.
