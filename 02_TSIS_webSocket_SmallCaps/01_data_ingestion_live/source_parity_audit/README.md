# Source Parity Audit

Official workspace for proving granular parity between:
- historical/raw Polygon evidence used for ML/RL training;
- live Polygon WebSocket fields;
- DAS/Sage CMD API fields available during realtime operation.

The audit must cite concrete files, commands, schemas, and sample rows before any live feature contract is declared usable.
